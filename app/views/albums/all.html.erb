<h1>Albums</h1>
this should show a list of all Albums


<table>
    <tr>
        <th>Artist</th>
        <th>Title</th>
        <th>Year</th>
        <th colspan="3"></th>
    </tr>

    <% @albums.each do |album| %>
        <tr>
            <td><%= album.artist.name %></td>
            <td><%= album.title %></td>
            <td><%= album.year %></td>
        </tr>
    <% end %>

</table>
<%= link_to 'Add Artist', new_artist_path() %>